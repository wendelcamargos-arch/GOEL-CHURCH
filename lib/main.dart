import 'package:flutter/widgets.dart';

import 'app/goel_app.dart';

/// Ponto de entrada da camada de entrega (Delivery Layer).
///
/// Slice 01 — Bootstrap. Nenhuma funcionalidade de slices posteriores é
/// inicializada aqui.
void main() {
  runApp(const GoelApp());
}
